#include "main.h"
#include <stdio.h>
/**
 * print_line - check the code.
 * @n: is a character.
 * Return: Always 0.
 */
void print_line(int n)
{
int i;
int j;
for (j = 1 ; j <= n ; j++)
{
for (i = 1 ; i <= n ; i++)
{
_putchar(35);
}
i = 1;
_putchar('\n');
}
