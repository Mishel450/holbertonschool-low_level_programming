#include "dog.h"
#include <string.h>
/**
 *
 *
 *
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	char name1 = malloc((char) * strlen(name) + 1);
	char owner1 = malloc((char) * strlen(owner) + 1);
}
