#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - check the code
 * @r: is a character
 * Return: Always 0.
 */
int _abs(int r)
{
int i;
i = abs(r);
_putchar("%d\n", i);
return (0);
}
