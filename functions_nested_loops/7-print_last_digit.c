#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - check the code
 * @i: is a character
 * Return: 0
 */
int print_last_digit(int i)
{
i = (i % 10);
printf("%d", i);
printf("%d", i);
printf("\n");
return (0);
}
