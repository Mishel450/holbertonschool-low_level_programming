#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - check the code
 * @i: is a character
 * Return: Always 0.
 */
int print_last_digit(int i)
{
i = (i % 10);
printf("%d%d\n" i, i);
return (0);
}
