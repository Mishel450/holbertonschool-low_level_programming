#include <stdio.h>
/**
 * main - entry point
 *
 * Return: 0
 */
int main(void)
{
char ch;
char ch2;
for (ch = 97 ; ch <= 122 ; ch++)
{
  putchar(ch);
}
for (ch2 = 41 ; ch2 <= 90 ; ch2++)
{
  putchar(ch2);
   }
 putchar('\n');
return (0);
}
