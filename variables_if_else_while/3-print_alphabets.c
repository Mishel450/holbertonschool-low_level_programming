#include <stdio.h>
/**
 * main - entry point
 *
 * Return: 0
 */
int main(void)
{
char ch;
char ch2;
 for (ch = 97 ; ch <= 122 ; ch++)
{
  putchar(ch);
}
for (ch2 = 41 ; ch <= 90 ; ch++)
{
  putchar(ch2);
   }
 putchar('\n');
return (0);
}
