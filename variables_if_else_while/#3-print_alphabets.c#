#include <stdio.h>
/**
 * main - entry point
 *
 * Return: 0
 */
int main(void)
{
char ch;
char ch2;
 for (ch = 'a' ; ch <= 'z' ; ch++)
{
putchar(ch);
if ()
{
for (ch2 = 'A' ; ch <= 'Z' ; ch++)
{
putchar(ch2);
}
}
}
putchar('\n');
return (0);
}
