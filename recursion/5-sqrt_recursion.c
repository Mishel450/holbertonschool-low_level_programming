#include "main."
/**
 *
 *
 *
 */
int main(int i)
{

	if (i * i == n)
	{
		return (i);
	}
	if (i == j)
	{
		return (-1);
	}
	if (i < n)
	{
		return (main(i + 1));
	}
}
/**
 *
 *
 *
 */
int _sqrt_recursion(int n)
{
	return (main(i));
}
