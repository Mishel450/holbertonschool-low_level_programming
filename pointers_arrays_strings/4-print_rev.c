#include "main.h"
/**
 * reset_to_98 - check the code.
 * @n: is a character
 * Return: Always 0.
 */
void print_rev(char *s)
{
char i;
for ( i = s ; i >= 0 ; i--)
{
_putchar(*s);
}
_putchar('\n');
}
